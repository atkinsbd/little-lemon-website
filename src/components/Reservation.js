import React from "react";
import BookingForm from "./BookingForm";

const Reservation = () => {
    return (
        <main>
            <BookingForm />
        </main>
    );
};

export default Reservation;